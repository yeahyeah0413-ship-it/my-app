@AGENTS.md

# 프로젝트 규칙 (CLAUDE.md)

> 초안입니다. 팀 논의를 거쳐 확정해주세요.

## 1. 프로젝트 개요

- 품의서·지출결의서 작성 가이드를 안내하고, 해결되지 않는 문의는 담당자에게 연결하는 사내용 챗봇 (PoC)
- 자세한 배경·목표·범위는 [PRD.md](./PRD.md) 참고

## 2. 기술 스택

- Next.js (App Router) — 화면과 API를 한 프로젝트에서 처리
- React 19 / TypeScript (strict 모드)
- Tailwind CSS v4
- ESLint 9 (`eslint-config-next`)
- 배포: Vercel 예정

## 3. 폴더 구조

- `app/` — App Router 기준 페이지·레이아웃
- `app/api/**/route.ts` — 서버 API는 Route Handler로 작성
- 이 프로젝트에서는 Pages Router(`pages/`)를 사용하지 않는다

## 4. 코딩 규칙

- TypeScript strict 모드를 유지하며 `any` 사용을 지양한다
- 클라이언트 상호작용이 필요한 컴포넌트에만 최상단에 `"use client"`를 명시한다
- 커밋 전 `npm run lint`로 통과 여부를 확인한다
- PoC 범위이므로 과도한 추상화·불필요한 라이브러리 추가를 지양하고 최대한 가볍게 구현한다

## 5. 보안 · 개인정보 (PRD 7번 참고)

- API 키 등 비밀 값은 `.env`에만 보관하고 코드에 하드코딩하지 않는다 (`.env`는 `.gitignore`에 등록되어 있음)
- 사내 공유용 가이드 문서만 참조 데이터로 사용하고, 대외비·기밀 문서는 사용하지 않는다
- 질문자의 소속·부서 정보만 활용하며, 이름 등 개인 식별정보는 저장하지 않는다

## 6. 범위 (PRD 6번 참고)

- 이번 PoC에서 만들지 않는 것: 품의서·지출결의서 자동 작성/제출, 결재 시스템 연동, 재무본부 외 타 부서 문의 대응, 실제 개인정보·회사 기밀 데이터 연동

## 7. 작업 규칙

- 모든 설명과 주석은 한국어로 작성한다
- 새 파일은 `my-app` 폴더 안에만 만든다
- 기술 스택은 PRD에 정한 대로 Next.js로 고정한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. 배포는 Vercel을 사용한다
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다
  - 예: Supabase를 쓸 상황이 생기면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다
- 코드 수정 작업을 마칠 때마다, 별도로 묻지 않고 바로 `git add` → `git commit` → `git push`까지 진행해 GitHub 원격 저장소(`origin`, `yeahyeah0413-ship-it/my-app`)에 반영한다
  - 커밋 메시지는 무엇을 왜 바꿨는지 한국어 한 줄로 작성한다
  - `.env`의 `GITHUB_TOKEN`으로 인증한다 (사용자에게 다시 묻지 않음)
